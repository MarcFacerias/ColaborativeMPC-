function samplegood = importSamples()
%IMPORTFILE Import data from a text file
%  SAMPLEGOOD = IMPORTFILE(FILENAME) reads data from text file FILENAME
%  for the default selection.  Returns the numeric data.
%
%  SAMPLEGOOD = IMPORTFILE(FILE, DATALINES) reads data for the specified
%  row interval(s) of text file FILENAME. Specify DATALINES as a
%  positive scalar integer or a N-by-2 array of positive scalar integers
%  for dis-contiguous row intervals.
%
%  Example:
%  samplegood = importfile("/home/marc/git/mcp_ros/mpc-tests-/puzzlebot_ws/src/mpc_controller/data/sample_good.csv", [3, Inf]);
%
%  See also READTABLE.
%
% Auto-generated by MATLAB on 11-Oct-2022 15:31:39

%% Input handling

% If dataLines is not specified, define defaults
if nargin < 2
    dataLines = [3, Inf];
end

%% Set up the Import Options and import the data
opts = delimitedTextImportOptions("NumVariables", 4);

% Specify range and delimiter
opts.DataLines = dataLines;
opts.Delimiter = ",";

% Specify column names and types
opts.VariableNames = ["Var1", "VarName2", "VarName3", "VarName4"];
opts.SelectedVariableNames = ["VarName2", "VarName3", "VarName4"];
opts.VariableTypes = ["string", "double", "double", "double"];

% Specify file level properties
opts.ExtraColumnsRule = "ignore";
opts.EmptyLineRule = "read";

% Specify variable properties
opts = setvaropts(opts, "Var1", "WhitespaceRule", "preserve");
opts = setvaropts(opts, "Var1", "EmptyFieldRule", "auto");

% Import the data
samplegood = readtable("/home/marc/git/mcp_ros/mpc-tests-/puzzlebot_ws/src/mpc_controller/data/sample_good.csv", opts);

%% Convert to output type
samplegood = table2array(samplegood);
end