function trajectorys1 = importRefS()
%IMPORTFILE Import data from a text file
%  TRAJECTORYS1 = IMPORTFILE(FILENAME) reads data from text file
%  FILENAME for the default selection.  Returns the numeric data.
%
%  TRAJECTORYS1 = IMPORTFILE(FILE, DATALINES) reads data for the
%  specified row interval(s) of text file FILENAME. Specify DATALINES as
%  a positive scalar integer or a N-by-2 array of positive scalar
%  integers for dis-contiguous row intervals.
%
%  Example:
%  trajectorys1 = importfile("/home/marc/git/mcp_ros/mpc-tests-/puzzlebot_ws/src/mpc_controller/references/trajectory_s.csv", [2, Inf]);
%
%  See also READTABLE.
%
% Auto-generated by MATLAB on 11-Oct-2022 15:43:29

%% Input handling

% If dataLines is not specified, define defaults
if nargin < 2
    dataLines = [2, Inf];
end

%% Set up the Import Options and import the data
opts = delimitedTextImportOptions("NumVariables", 8);

% Specify range and delimiter
opts.DataLines = dataLines;
opts.Delimiter = ",";

% Specify column names and types
opts.VariableNames = ["x", "y", "th", "Var4", "Var5", "Var6", "Var7", "Var8"];
opts.SelectedVariableNames = ["x", "y", "th"];
opts.VariableTypes = ["double", "double", "double", "string", "string", "string", "string", "string"];

% Specify file level properties
opts.ExtraColumnsRule = "ignore";
opts.EmptyLineRule = "read";

% Specify variable properties
opts = setvaropts(opts, ["Var4", "Var5", "Var6", "Var7", "Var8"], "WhitespaceRule", "preserve");
opts = setvaropts(opts, ["Var4", "Var5", "Var6", "Var7", "Var8"], "EmptyFieldRule", "auto");

% Import the data
trajectorys1 = readtable("/home/marc/git/mcp_ros/mpc-tests-/puzzlebot_ws/src/mpc_controller/references/trajectory_s.csv", opts);

%% Convert to output type
trajectorys1 = table2array(trajectorys1);
end